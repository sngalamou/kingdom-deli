{
  "name": "ngc-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Ngalamou Consultancy productized client delivery monorepo. Shared packages + per-client workspaces.",
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check": "turbo run check",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "prettier": "^3.3.0",
    "turbo": "^2.1.3",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": "22.x",
    "pnpm": ">=11.22.0"
  }
}
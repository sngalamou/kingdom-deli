#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../gunzip-maybe/bin.js" "$@"
else
  exec node  "$basedir/../../../gunzip-maybe/bin.js" "$@"
fi
# cmd-shim-target=C:/Users/sngalamou/dev/ngc/kingdom-deli/node_modules/.pnpm/sanity@3.99.0_@emotion+is-p_bf25d5489aa395ad89ac3a44d57982da/node_modules/gunzip-maybe/bin.js

#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../nanoid/bin/nanoid.js" "$@"
else
  exec node  "$basedir/../../../../nanoid/bin/nanoid.js" "$@"
fi
# cmd-shim-target=C:/Users/sngalamou/dev/ngc/kingdom-deli/node_modules/.pnpm/@sanity+mutate@0.12.6/node_modules/nanoid/bin/nanoid.js

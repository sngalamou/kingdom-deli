import { ClientConfig } from '@sanity/client';
type PluginOptions = {
    logClientRequests?: 'dev' | 'build' | 'always';
};
export declare function vitePluginSanityClient(config: ClientConfig, options?: PluginOptions): {
    name: string;
    resolveId(this: import('rollup').PluginContext, id: string): string;
    load(this: import('rollup').PluginContext, id: string): string;
};
export {};
